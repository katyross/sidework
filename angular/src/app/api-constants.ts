import {environment} from "../environments/environment";

export class ApiConstants {
  public static SHIFT_ENDPOINT = environment.SHIFT_ENDPOINT;
  public static RESTAURANT_ENDPOINT = environment.RESTAURANT_ENDPOINT;
}
